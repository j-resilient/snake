class Snake {
    // store snake directions - up, down, right, left
    constructor() {
        // store snake segments (grid coordinates - presumably where each part of the snake is at the current moment)
        // starting position???
        // store the current direction
    }

    move() {
        // move the snake in the current direction
    }

    turn(dir) {
        // change the current direction
    }

    _plus(c1, c2) {
        // sum and return the coordinates
    }

    _equals(c1, c2) {
        // check if the coordinates are equal??
    }
}